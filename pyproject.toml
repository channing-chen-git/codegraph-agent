[project]
name = "codegraph-agent"
version = "0.1.0"
description = "Repository-level code understanding agent with symbol graphs, impact analysis and offline evaluation."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
api = ["fastapi>=0.110", "uvicorn>=0.29", "pydantic>=2"]

[tool.ruff]
line-length = 100
